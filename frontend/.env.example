# Resend — case emails (3,000/month free, no Firebase Blaze needed)
RESEND_API_KEY=
RESEND_FROM_ADDRESS=SpeakUp GC <noreply@resend.dev>

# Firebase Web Push (PWA mobile lock-screen notifications)
# Firebase Console → Project settings → Cloud Messaging → Web Push certificates → Generate key pair
NEXT_PUBLIC_FIREBASE_VAPID_KEY=

# Firebase Admin (for /api/notifications/send-push — no Blaze required)
# Project settings → Service accounts → Generate new private key
FIREBASE_PROJECT_ID=speakupgc-2026
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
